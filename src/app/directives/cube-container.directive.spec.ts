import { CubeContainerDirective } from './cube-container.directive';

describe('CubeContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CubeContainerDirective();
    expect(directive).toBeTruthy();
  });
});
